# Qwen多卡训练系统依赖包
# 安装命令: pip install -r requirements_migration.txt

# 核心深度学习框架
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Hugging Face生态系统
transformers>=4.35.0
datasets>=2.14.0
acceleratate>=0.24.0
tokenizers>=0.14.0

# Flash Attention (需要CUDA)
flash-attn>=2.3.0

# 训练和评估工具
tensorboard>=2.14.0
wandb>=0.15.0  # 可选，用于实验跟踪

# 数据处理
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# 进度条和日志
tqdm>=4.65.0
rich>=13.0.0  # 可选，用于美化输出

# 配置文件处理
pyyaml>=6.0
toml>=0.10.0

# 系统监控
psutil>=5.9.0
gpustat>=1.1.0  # 可选，用于GPU监控

# 开发和调试工具
ipython>=8.0.0
jupyter>=1.0.0  # 可选，用于交互式开发

# 数据可视化
matplotlib>=3.7.0
seaborn>=0.12.0  # 可选，用于高级可视化

# 网络和下载
requests>=2.31.0
urllib3>=2.0.0

# 多进程和并行处理
multiprocess>=0.70.0
joblib>=1.3.0

# 内存优化
memory-profiler>=0.61.0  # 可选，用于内存分析

# 模型优化
bitsandbytes>=0.41.0  # 可选，用于量化

# 文本处理
sentencepiece>=0.1.99
protobuf>=4.24.0

# 实用工具
click>=8.1.0
typer>=0.9.0  # 可选，用于命令行界面
pathlib2>=2.3.0  # Python < 3.4兼容性

# 测试工具（开发用）
pytest>=7.4.0
pytest-cov>=4.1.0

# 代码质量工具（开发用）
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0

# 类型检查（开发用）
mypy>=1.5.0
types-requests>=2.31.0