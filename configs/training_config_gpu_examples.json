{
  "_comment": "GPU选择配置示例文件",
  "_description": "展示如何在训练配置中指定GPU使用方式",
  
  "example_1_auto_selection": {
    "description": "自动选择所有可用GPU",
    "training_args": {
      "gpu_ids": null,
      "gpu_type": null
    }
  },
  
  "example_2_gpu_type_selection": {
    "description": "按GPU类型选择（优先使用A800）",
    "training_args": {
      "gpu_ids": null,
      "gpu_type": "A800"
    }
  },
  
  "example_3_manual_gpu_selection": {
    "description": "手动指定使用特定的GPU ID",
    "training_args": {
      "gpu_ids": [0, 1, 2, 3],
      "gpu_type": null
    }
  },
  
  "example_4_mixed_gpu_selection": {
    "description": "使用混合GPU（A800和A40）",
    "training_args": {
      "gpu_ids": [0, 1, 4, 5],
      "gpu_type": null,
      "_note": "假设0,1是A800，4,5是A40"
    }
  },
  
  "example_5_single_gpu": {
    "description": "单GPU训练",
    "training_args": {
      "gpu_ids": [0],
      "gpu_type": null
    }
  },
  
  "example_6_a40_only": {
    "description": "仅使用A40 GPU",
    "training_args": {
      "gpu_ids": null,
      "gpu_type": "A40"
    }
  },
  
  "usage_instructions": {
    "command_line": {
      "description": "命令行方式指定GPU",
      "examples": [
        "python train_qwen_multi_gpu.py --gpu_ids 0 1 2 3",
        "python train_qwen_multi_gpu.py --gpu_type A800",
        "./run_training.sh 1 A800 my_experiment",
        "./run_training.sh 1 manual my_experiment \"0,1,2,3\""
      ]
    },
    "config_file": {
      "description": "配置文件方式",
      "steps": [
        "1. 复制 training_config.json 为新文件",
        "2. 修改其中的 gpu_ids 或 gpu_type 字段",
        "3. 使用 --config 参数指定配置文件"
      ]
    }
  },
  
  "gpu_mapping_reference": {
    "description": "当前服务器GPU映射参考（基于测试结果）",
    "gpu_layout": {
      "0": "NVIDIA A800 80GB PCIe",
      "1": "NVIDIA A800 80GB PCIe",
      "2": "NVIDIA A800 80GB PCIe",
      "3": "NVIDIA A800 80GB PCIe",
      "4": "NVIDIA A40",
      "5": "NVIDIA A40",
      "6": "NVIDIA A40",
      "7": "NVIDIA A40"
    },
    "recommendations": {
      "high_memory_tasks": "使用A800 (GPU 0-3)",
      "balanced_tasks": "混合使用A800和A40",
      "multi_experiment": "分别使用不同GPU组避免冲突"
    }
  }
}